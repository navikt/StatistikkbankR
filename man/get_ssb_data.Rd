% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ssb_data.r
\name{get_ssb_data}
\alias{get_ssb_data}
\title{Download data from Statistics Norway (SSB)}
\usage{
get_ssb_data(
  table_id,
  ...,
  language = "no",
  as_tibble = TRUE,
  override_large_query = FALSE,
  table_format = "wide",
  include_singleton_dims = FALSE,
  include_status = FALSE,
  character_as_factor = TRUE,
  convert_quarter_to_yearqtr = TRUE,
  convert_month_to_yearmon = TRUE,
  codelists = NULL,
  output_values = NULL,
  cache = TRUE,
  refresh_metadata = FALSE,
  max_get_query_chars = 1500L,
  show_chunk_progress = FALSE
)
}
\arguments{
\item{table_id}{Character.  The SSB table identifier, for example
\code{"07459"}.  You can find identifiers by searching on
\url{https://www.ssb.no/statbank} or by calling \code{\link[=ssb_search]{ssb_search()}}.}

\item{...}{Filters that limit which rows are downloaded.  Each filter
is a named argument whose name is a dimension identifier (exactly as
shown by \code{\link[=ssb_describe]{ssb_describe()}}) and whose value is a character vector of
the codes you want.  If you supply no filters the entire table is
downloaded.

You may also use server-side selection expressions instead of
listing codes one by one:
\describe{
\item{\code{"*"}}{All available values for that dimension.}
\item{\code{"top(n)"}}{The \emph{n} most recent values (useful for time
dimensions).}
\item{\code{"bottom(n)"}}{The \emph{n} oldest values.}
\item{\code{"from(code)"}}{All values from \emph{code} onwards.}
\item{\code{"to(code)"}}{All values up to and including \emph{code}.}
\item{\code{"range(a, b)"}}{All values between \emph{a} and \emph{b}.}
}}

\item{language}{Character.  The language used for column and value
labels in the result.  Use \code{"no"} for Norwegian (the default) or
\code{"en"} for English.}

\item{as_tibble}{Logical.  If \code{TRUE} (the default), the result is
returned as a \link[tibble:tibble]{tibble}.  Set to \code{FALSE} to get a
plain \code{data.frame}.}

\item{override_large_query}{Logical.  SSB limits how many data cells
a single request may return (typically 800,000).  If your query
would exceed this limit, the function stops with an informative
error.  Set \code{override_large_query = TRUE} to skip the check and send
the request anyway.  Use with care on very large tables.}

\item{table_format}{Character.  Shape of the returned data frame.
\code{"wide"} (the default) places each measure in its own column.
\code{"long"} returns one row per value with separate \code{dimension},
\code{code}, \code{label}, and \code{value} columns, which is convenient for
plotting with ggplot2 or for tables with many measures.}

\item{include_singleton_dims}{Logical.  Only used when
\code{table_format = "long"}.  If \code{FALSE} (the default), dimensions that
have only one selected value are dropped from the long-format output
to keep it concise.  Set to \code{TRUE} to keep all dimensions
regardless.}

\item{include_status}{Logical.  Some cells carry a status flag such as
\code{"c"} (confidential) or \code{"."} (not applicable) instead of a numeric
value.  If \code{TRUE}, a \code{status} column is added to the result
containing these flags.  Defaults to \code{FALSE}.}

\item{character_as_factor}{Logical.  If \code{TRUE} (the default),
dimension label columns are returned as factors, which preserves the
ordering that SSB defines for the codes.  Set to \code{FALSE} to keep
them as plain character strings.}

\item{convert_quarter_to_yearqtr}{Logical.  If \code{TRUE} (the default),
quarterly time codes such as \code{"2024K1"} are converted to
\link[zoo:yearqtr]{zoo::yearqtr} objects, which are suitable for time series analysis.
Set to \code{FALSE} to keep the original SSB code strings.}

\item{convert_month_to_yearmon}{Logical.  If \code{TRUE} (the default),
monthly time codes such as \code{"2024M01"} are converted to
\link[zoo:yearmon]{zoo::yearmon} objects, which are suitable for time series analysis.
Set to \code{FALSE} to keep the original SSB code strings.}

\item{codelists}{Named list.  Applies an SSB aggregation grouping
(codelist) to one or more dimensions.  This is how you request data
at an aggregated geographic or administrative level — for example,
using a summer municipality boundary instead of current municipality
codes.  The name of each entry must match a dimension identifier and
the value must be a codelist identifier.  Use \code{\link[=ssb_codelists]{ssb_codelists()}} to
discover which codelists are available for a table.
Example: \code{list(Region = "agg_KommSummer")}.}

\item{output_values}{Named list.  When a codelist groups codes
together, this argument controls whether the result shows the
aggregated group value (\code{"aggregated"}, one row per group) or all
the individual member codes (\code{"single"}, one row per member).
Each name must match a dimension that also appears in \code{codelists}.
Example: \code{list(Region = "aggregated")}.}

\item{cache}{Logical.  If \code{TRUE} (the default), table metadata
(dimension names, valid codes, etc.) is stored in memory for the
duration of the R session.  This avoids repeated network round-trips
when you call the function multiple times for the same table.}

\item{refresh_metadata}{Logical.  If \code{TRUE}, any cached metadata for
this table is discarded and fresh metadata is fetched from SSB.
Useful if you know the table definition has changed.  Defaults to
\code{FALSE}.}

\item{max_get_query_chars}{Numeric.  Internal tuning parameter.  When
the encoded filter string is shorter than this limit (in bytes) the
request is sent as a standard GET request; when longer, it is sent
as a POST request to avoid URL length restrictions.  The default
value of \code{1500L} works for the vast majority of queries and rarely
needs to be changed.}

\item{show_chunk_progress}{Logical.  Deprecated.  Has no effect and
will be removed in a future version.}
}
\value{
A \link[tibble:tibble]{tibble} (or \code{data.frame} when
\code{as_tibble = FALSE}) where each row is one observation.  In wide
format, dimension label columns appear first followed by one numeric
column per measure.  In long format there are four fixed columns:
\code{dimension}, \code{code}, \code{label}, and \code{value}.  When
\code{include_status = TRUE} an additional \code{status} column follows
\code{value}.
}
\description{
Downloads one or more variables from an SSB Statistikkbank table and
returns them as a tidy data frame.  Supply the table identifier and,
optionally, filters that narrow the download to the rows you need.
Use \code{\link[=ssb_search]{ssb_search()}} to find table identifiers and \code{\link[=ssb_describe]{ssb_describe()}} to
inspect the dimensions and valid codes for a table before downloading.
}
\section{Finding tables and dimensions}{

Use \code{\link[=ssb_search]{ssb_search()}} to search table titles by keyword and
\code{\link[=ssb_describe]{ssb_describe()}} to list the dimensions of a specific table together
with their roles and the number of available codes.  Once you know the
dimension identifiers, call \code{\link[=ssb_codes]{ssb_codes()}} to list the exact code
strings that can be used as filter values.
}

\section{Filtering}{

Filter arguments are matched by dimension identifier.  Identifiers are
case-sensitive and must be spelled exactly as returned by
\code{\link[=ssb_describe]{ssb_describe()}}.  If you omit a dimension, all of its codes are
included automatically.  You can mix explicit code vectors and
server-side expressions in the same call.
}

\section{Handling large tables}{

SSB imposes a cell limit on each request.  The function estimates the
number of cells your query will return before sending it and stops
early with a clear message if the limit would be exceeded.  Strategies
for staying within the limit: supply filters to reduce the number of
rows, use \code{"top(n)"} on the time dimension, or split the download into
several calls with different filter values.  When the query is small
enough, the function sends it as a GET request; for larger but
still-valid queries it switches to a POST request automatically.
}

\section{Codelists and geographic aggregation}{

SSB publishes aggregation codelists that map individual municipality
or region codes to higher-level groupings such as county or historical
boundary sets.  Pass the codelist identifier in \code{codelists} to use
one.  The \code{output_values} argument then controls whether you receive
the aggregated label for each group (\code{"aggregated"}) or each
individual member code (\code{"single"}).  See \code{\link[=ssb_codelists]{ssb_codelists()}} and
\code{\link[=ssb_get_by_codelist]{ssb_get_by_codelist()}} for discovery and convenience wrappers.
}

\section{Time formatting}{

SSB encodes quarters in the form \code{"YYYYKn"} (e.g., \code{"2024K1"}) and
months in the form \code{"YYYYMmm"} (e.g., \code{"2024M01"}). By default,
quarterly codes are converted to \link[zoo:yearqtr]{zoo::yearqtr} and monthly codes are
converted to \link[zoo:yearmon]{zoo::yearmon}. Set \code{convert_quarter_to_yearqtr = FALSE}
and/or \code{convert_month_to_yearmon = FALSE} to keep original SSB code
strings.
}

\examples{
\dontrun{
# --- 1. Download an entire small table --------------------------------
dat <- get_ssb_data("07459")

# --- 2. Download with filters -----------------------------------------
# Use ssb_describe("07459") and ssb_codes("07459", "Region")
# to find valid dimension names and code values first.
dat <- get_ssb_data(
    "07459",
    Region = c("0301", "1103"),
    Tid    = c("2023", "2024")
)

# --- 3. Server-side selection expressions -----------------------------
# "top(3)" on Tid keeps only the three most recent years.
# "*" on Kjonn selects all available sex categories.
dat <- get_ssb_data(
    "07459",
    Tid   = "top(3)",
    Kjonn = "*"
)

# --- 4. Long format for plotting --------------------------------------
dat_long <- get_ssb_data(
    "07459",
    Tid        = "top(5)",
    table_format = "long"
)

# --- 5. English labels ------------------------------------------------
dat_en <- get_ssb_data(
    "07459",
    Tid      = "top(3)",
    language = "en"
)

# --- 6. Codelist: aggregated regions ----------------------------------
# Download population using historical summer municipality boundary.
# output_values = "aggregated" returns one row per boundary group.
dat_agg <- get_ssb_data(
    "07459",
    Tid          = "top(3)",
    codelists    = list(Region = "agg_KommSummer"),
    output_values = list(Region = "aggregated")
)

# --- 7. Status flags --------------------------------------------------
# Keep the status column to see suppressed or missing cells.
dat_status <- get_ssb_data(
    "07459",
    Tid            = "top(3)",
    include_status = TRUE
)

# --- 8. Quarterly time values as zoo::yearqtr ---------------------------
# Table 12452 has quarterly time codes such as "2024K1".
# By default, these are converted to zoo::yearqtr objects.
dat_q <- get_ssb_data(
    "12452",
    Tid = "top(4)"
)

# --- 9. Monthly time values as zoo::yearmon ---------------------------
# Table 13966 has monthly time codes such as "2024M01".
# By default, these are converted to zoo::yearmon objects.
# dat_m <- get_ssb_data(
#     "13966",
#     Tid = "top(4)"
# )
}

}
\seealso{
\code{\link[=ssb_search]{ssb_search()}} to find table identifiers by keyword.
\code{\link[=ssb_describe]{ssb_describe()}} to inspect dimensions before downloading.
\code{\link[=ssb_codes]{ssb_codes()}} to list valid filter values for a dimension.
\code{\link[=ssb_codelists]{ssb_codelists()}} to discover available aggregation groupings.
\code{\link[=ssb_get_by_codelist]{ssb_get_by_codelist()}} as a shortcut that resolves codelist
dimension mapping automatically.
}
