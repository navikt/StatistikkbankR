% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.r
\name{ssb_codes}
\alias{ssb_codes}
\title{List valid codes for one dimension of an SSB table}
\usage{
ssb_codes(
  table_id,
  dimension,
  language = "no",
  cache = TRUE,
  refresh_metadata = FALSE
)
}
\arguments{
\item{table_id}{Character.  The SSB table identifier, for example
\code{"07459"}.}

\item{dimension}{Character.  The dimension identifier, exactly as
shown in the \code{dimension} column of \code{\link[=ssb_describe]{ssb_describe()}}.  For example
\code{"Region"} or \code{"Tid"}.}

\item{language}{Character.  Language for labels.  \code{"no"} (default)
or \code{"en"}.}

\item{cache}{Logical.  If \code{TRUE} (the default), table metadata is
stored in memory for the session so that repeated calls for the
same table are fast.}

\item{refresh_metadata}{Logical.  If \code{TRUE}, discard any cached
metadata and fetch fresh information from SSB.  Defaults to
\code{FALSE}.}
}
\value{
A \code{data.frame} with four columns: \code{table_id}, \code{dimension},
\code{code} (the value to use in a filter), and \code{label} (the
human-readable name).
}
\description{
Returns every code that is accepted as a filter value for one
dimension of the given table, together with the human-readable label
for each code.  Use this before calling \code{\link[=get_ssb_data]{get_ssb_data()}} to verify
that the codes you want to filter on are valid.
}
\examples{
\dontrun{
# Inspect all dimensions of table 07459
ssb_describe("07459")

# List every valid code for the Region dimension
ssb_codes("07459", "Region")

# Same in English
ssb_codes("07459", "Region", language = "en")
}
}
\seealso{
\code{\link[=ssb_describe]{ssb_describe()}} to list all dimensions first.
\code{\link[=get_ssb_data]{get_ssb_data()}} to download data using the codes as filters.
}
