% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.r
\name{ssb_codelists}
\alias{ssb_codelists}
\title{List available codelists for an SSB table}
\usage{
ssb_codelists(
  table_id,
  dimension = NULL,
  language = "no",
  cache = TRUE,
  refresh_metadata = FALSE
)
}
\arguments{
\item{table_id}{Character.  The SSB table identifier.}

\item{dimension}{Optional character.  If supplied, only codelists for
that dimension are returned.  Must match a dimension identifier
exactly as shown by \code{\link[=ssb_describe]{ssb_describe()}}.}

\item{language}{Character.  Language for labels.  \code{"no"} (default)
or \code{"en"}.}

\item{cache}{Logical.  If \code{TRUE} (the default), table metadata is
stored in memory for the session.}

\item{refresh_metadata}{Logical.  If \code{TRUE}, discard any cached
metadata and fetch fresh information from SSB.  Defaults to
\code{FALSE}.}
}
\value{
A \code{data.frame} with one row per codelist, containing columns
\code{table_id}, \code{dimension}, \code{dimension_label}, \code{codelist_id},
\code{codelist_label}, \code{codelist_type}, \code{supports_output_values},
\code{suggested_output_values}, and \code{codelist_href}.
If no codelists exist, an empty \code{data.frame} with the same
structure is returned.
}
\description{
Returns the aggregation codelists that SSB provides for each
dimension of a table.  A codelist lets you request data at a
different grouping level without manually remapping codes — for
example, using a historical municipality boundary instead of current
municipality codes.  Use this to discover which codelists exist
before passing one to \code{\link[=get_ssb_data]{get_ssb_data()}} via the \code{codelists} argument.
}
\details{
The \code{supports_output_values} column indicates whether the codelist
supports the \code{output_values} argument in \code{\link[=get_ssb_data]{get_ssb_data()}}.  When
\code{TRUE}, you can pass \code{"aggregated"} to receive one row per group or
\code{"single"} to receive one row per underlying member code.  The
\code{suggested_output_values} column shows the accepted values as a
convenience reminder.
}
\examples{
\dontrun{
# List all codelists for table 07459
ssb_codelists("07459")

# Filter to a specific geographic dimension
ssb_codelists("07459", dimension = "Region")
}
}
\seealso{
\code{\link[=ssb_codelist_details]{ssb_codelist_details()}} to inspect the individual codes inside a
specific codelist.
\code{\link[=ssb_get_by_codelist]{ssb_get_by_codelist()}} to download data using a codelist without
specifying the dimension manually.
\code{\link[=get_ssb_data]{get_ssb_data()}} for the \code{codelists} and \code{output_values} arguments.
}
