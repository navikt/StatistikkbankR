% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.r
\name{ssb_get_by_codelist}
\alias{ssb_get_by_codelist}
\title{Download SSB data using a codelist without specifying the dimension}
\usage{
ssb_get_by_codelist(
  table_id,
  codelist_id,
  ...,
  dimension = NULL,
  output_value = NULL,
  language = "no",
  as_tibble = TRUE,
  override_large_query = FALSE,
  table_format = "wide",
  include_singleton_dims = FALSE,
  cache = TRUE,
  refresh_metadata = FALSE,
  max_get_query_chars = 1500L,
  show_chunk_progress = FALSE
)
}
\arguments{
\item{table_id}{Character.  The SSB table identifier, for example
\code{"07459"}.}

\item{codelist_id}{Character.  The codelist identifier to apply,
for example \code{"agg_KommSummer"}.  Use \code{\link[=ssb_codelists]{ssb_codelists()}} to find
available identifiers for a table.}

\item{...}{Filters for table dimensions, passed directly to
\code{\link[=get_ssb_data]{get_ssb_data()}}.  Each filter is a named argument whose name is
a dimension identifier and whose value is a character vector of
codes or a server-side expression such as \code{"top(3)"}.}

\item{dimension}{Optional character.  Only needed if the same
codelist identifier is used on more than one dimension in the
table.  Specify the dimension identifier explicitly to resolve the
ambiguity.}

\item{output_value}{Optional character.  Controls how the codelist
dimension appears in the result.  \code{"aggregated"} (default when
supported) returns one row per group defined in the codelist.
\code{"single"} returns one row per underlying member code.  Leave
\code{NULL} to use the SSB default.  Only applicable when the codelist
supports output values (see \code{supports_output_values} in
\code{\link[=ssb_codelists]{ssb_codelists()}}).}

\item{language}{Character.  Language for labels.  \code{"no"} (default)
or \code{"en"}.}

\item{as_tibble}{Logical.  If \code{TRUE} (the default), the result is
returned as a \link[tibble:tibble]{tibble}.}

\item{override_large_query}{Logical.  If \code{TRUE}, bypass the cell
limit guard.  See \code{\link[=get_ssb_data]{get_ssb_data()}} for details.}

\item{table_format}{Character.  \code{"wide"} (default) or \code{"long"}.
See \code{\link[=get_ssb_data]{get_ssb_data()}} for details.}

\item{include_singleton_dims}{Logical.  Only used with
\code{table_format = "long"}.  See \code{\link[=get_ssb_data]{get_ssb_data()}} for details.}

\item{cache}{Logical.  If \code{TRUE} (the default), table metadata is
cached in memory for the session.}

\item{refresh_metadata}{Logical.  If \code{TRUE}, discard cached
metadata and re-fetch from SSB.  Defaults to \code{FALSE}.}

\item{max_get_query_chars}{Numeric.  Internal tuning parameter;
see \code{\link[=get_ssb_data]{get_ssb_data()}} for details.  Defaults to \code{1500L}.}

\item{show_chunk_progress}{Logical.  Deprecated.  Has no effect.}
}
\value{
A \link[tibble:tibble]{tibble} (or \code{data.frame} when
\code{as_tibble = FALSE}) as described in \code{\link[=get_ssb_data]{get_ssb_data()}}.
}
\description{
A convenience wrapper around \code{\link[=get_ssb_data]{get_ssb_data()}} that automatically
looks up which dimension a codelist belongs to and applies it for
you.  Use this instead of \code{\link[=get_ssb_data]{get_ssb_data()}} when you know the
codelist identifier you want to use but do not want to manually
identify the matching dimension.
}
\details{
The function calls \code{\link[=ssb_codelists]{ssb_codelists()}} internally to confirm that the
codelist identifier exists for the table and to determine which
dimension it belongs to.  It then calls \code{\link[=get_ssb_data]{get_ssb_data()}} with the
correct \code{codelists} and \code{output_values} arguments filled in
automatically.

If the codelist identifier is present on more than one dimension,
the function stops with a message listing the affected dimensions
so you can pass the correct one via \code{dimension}.
}
\examples{
\dontrun{
# Download population aggregated by summer municipality boundary.
# The function detects that "agg_KommSummer" belongs to Region.
pop <- ssb_get_by_codelist(
    "07459",
    "agg_KommSummer",
    Tid          = "top(3)",
    output_value = "aggregated"
)

# Request individual member codes instead of aggregated groups
pop_single <- ssb_get_by_codelist(
    "07459",
    "agg_KommSummer",
    Tid          = "top(3)",
    output_value = "single"
)

# When the codelist exists on multiple dimensions, specify which one
pop_explicit <- ssb_get_by_codelist(
    "07459",
    "agg_KommSummer",
    Tid          = "top(3)",
    dimension    = "Region",
    output_value = "aggregated"
)
}
}
\seealso{
\code{\link[=get_ssb_data]{get_ssb_data()}} for the full set of download options.
\code{\link[=ssb_codelists]{ssb_codelists()}} to discover available codelists for a table.
\code{\link[=ssb_codelist_details]{ssb_codelist_details()}} to inspect the group-to-member mapping.
\code{\link[=ssb_expand_codelist_mapping]{ssb_expand_codelist_mapping()}} to expand aggregated result rows
to individual member codes after downloading.
}
