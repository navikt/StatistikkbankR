% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.r
\name{ssb_codelist_details}
\alias{ssb_codelist_details}
\title{Inspect the members of an SSB codelist}
\usage{
ssb_codelist_details(codelist_id, language = "no", as_tibble = TRUE)
}
\arguments{
\item{codelist_id}{Character.  The codelist identifier, for example
\code{"agg_KommSummer"}.  Find available identifiers with
\code{\link[=ssb_codelists]{ssb_codelists()}}.}

\item{language}{Character.  Language for labels.  \code{"no"} (default)
or \code{"en"}.}

\item{as_tibble}{Logical.  If \code{TRUE} (the default), the \verb{$values}
element of the returned list is a \link[tibble:tibble]{tibble}.}
}
\value{
A named list with two elements:
\describe{
\item{\verb{$codelist}}{A list with metadata about the codelist:
\code{id}, \code{label}, \code{type} (e.g. \code{"Aggregation"}), \code{language},
\code{elimination} (logical), and \code{elimination_value_code}.}
\item{\verb{$values}}{A \code{data.frame} or tibble with one row per group
code, containing \code{code} (the group code), \code{label} (the group
name), \code{mapped_values} (a comma-separated string of the member
codes that belong to this group), and \code{mapped_count} (how many
members the group contains).}
}
}
\description{
Downloads the full definition of a single codelist — the group codes
it defines and which underlying member codes each group contains.
Use this to understand exactly how a codelist maps individual codes
to higher-level groups before using it in \code{\link[=get_ssb_data]{get_ssb_data()}}.
}
\details{
A codelist groups individual codes (for example, municipality codes)
into aggregate units (for example, historical municipality boundaries
or counties).  This function lets you inspect those groupings
directly.
}
\examples{
\dontrun{
# Inspect the summer municipality boundary codelist
cl <- ssb_codelist_details("agg_KommSummer")

# See top-level metadata
cl$codelist

# View the group-to-member mapping
cl$values
}
}
\seealso{
\code{\link[=ssb_codelists]{ssb_codelists()}} to list which codelists are available for a table.
\code{\link[=ssb_expand_codelist_mapping]{ssb_expand_codelist_mapping()}} to join the member codes back onto
a data frame you have already downloaded.
}
