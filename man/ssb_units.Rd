% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.r
\name{ssb_units}
\alias{ssb_units}
\title{List measurement units for an SSB table}
\usage{
ssb_units(
  table_id,
  dimension = NULL,
  language = "no",
  cache = TRUE,
  refresh_metadata = FALSE
)
}
\arguments{
\item{table_id}{Character.  The SSB table identifier, for example
\code{"07459"}.}

\item{dimension}{Optional character.  The dimension identifier to
filter results to a single dimension.  If \code{NULL} (the default),
units for all dimensions are returned.}

\item{language}{Character.  Language for labels.  \code{"no"} (default)
or \code{"en"}.}

\item{cache}{Logical.  If \code{TRUE} (the default), table metadata is
stored in memory for the session.}

\item{refresh_metadata}{Logical.  If \code{TRUE}, discard any cached
metadata and fetch fresh information from SSB.  Defaults to
\code{FALSE}.}
}
\value{
A \code{data.frame} with columns \code{table_id}, \code{dimension},
\code{dimension_label}, \code{code}, \code{label}, \code{unit_base} (the unit name,
e.g. \code{"persons"}), and \code{unit_decimals} (the number of decimal
places SSB recommends for display).
}
\description{
Returns unit information (such as \code{"persons"}, \code{"NOK"}, or
\code{"per cent"}) for the codes in a table's measure dimension.  Most
tables expose units on the contents/measure dimension; geographic and
time dimensions typically have none.
}
\details{
Not every code in a measure dimension has a unit; codes without
unit metadata are excluded from the result.  If a table has no unit
metadata at all, an empty \code{data.frame} with the correct column
structure is returned rather than an error.
}
\examples{
\dontrun{
# List all units for table 07459
ssb_units("07459")

# Filter to the contents dimension only
ssb_units("07459", dimension = "ContentsCode")
}
}
\seealso{
\code{\link[=ssb_describe]{ssb_describe()}} to check whether a dimension has units before
calling this function (the \code{has_units} column).
}
