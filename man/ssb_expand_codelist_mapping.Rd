% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.r
\name{ssb_expand_codelist_mapping}
\alias{ssb_expand_codelist_mapping}
\title{Expand grouped codelist codes to individual member codes}
\usage{
ssb_expand_codelist_mapping(
  data,
  code_col,
  codelist_id,
  language = "no",
  member_col = "member_code",
  keep_unmatched = TRUE,
  as_tibble = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} or tibble that contains a column of
grouped codelist codes.}

\item{code_col}{Character.  The name of the column in \code{data} that
holds the grouped codes, for example \code{"Region"}.}

\item{codelist_id}{Character.  The codelist identifier that defines
the grouping, for example \code{"agg_KommSummer"}.}

\item{language}{Character.  Language used when fetching codelist
details.  \code{"no"} (default) or \code{"en"}.}

\item{member_col}{Character.  The name of the new column to add to
the result that will contain the expanded member codes.  Defaults
to \code{"member_code"}.  Must not already exist in \code{data}.}

\item{keep_unmatched}{Logical.  If \code{TRUE} (the default), rows whose
grouped code is not found in the codelist are kept in the result
with \code{NA} in \code{member_col}.  If \code{FALSE}, those rows are silently
dropped.}

\item{as_tibble}{Logical.  If \code{TRUE} (the default), the result is
returned as a \link[tibble:tibble]{tibble}.}
}
\value{
A \link[tibble:tibble]{tibble} (or \code{data.frame} when
\code{as_tibble = FALSE}) with the same columns as \code{data} plus one new
column named by \code{member_col}.  The number of rows may be larger
than the input when groups contain multiple members.
}
\description{
Takes a data frame that contains grouped codelist codes (for example,
the aggregated region codes returned when using
\code{output_values = "aggregated"} in \code{\link[=get_ssb_data]{get_ssb_data()}}) and expands each
row into one row per individual member code.  This makes it easy to
join SSB aggregate data onto a dataset that uses the underlying
individual codes.
}
\details{
Each row in \code{data} may expand into multiple rows if its grouped code
maps to several member codes.  All original columns are duplicated
accordingly.  If a grouped code maps to no members and
\code{keep_unmatched = TRUE}, the row is kept with \code{NA} in \code{member_col};
with \code{keep_unmatched = FALSE} it is dropped.
}
\examples{
\dontrun{
# Download population aggregated by summer municipality boundary
pop_agg <- get_ssb_data(
    "07459",
    Tid           = "top(1)",
    codelists     = list(Region = "agg_KommSummer"),
    output_values = list(Region = "aggregated")
)

# Expand each group row to one row per member municipality code
pop_expanded <- ssb_expand_codelist_mapping(
    data        = pop_agg,
    code_col    = "Region",
    codelist_id = "agg_KommSummer"
)

# Rows whose group code has no members are kept with NA
# by default; set keep_unmatched = FALSE to drop them
pop_expanded_clean <- ssb_expand_codelist_mapping(
    data            = pop_agg,
    code_col        = "Region",
    codelist_id     = "agg_KommSummer",
    keep_unmatched  = FALSE
)
}
}
\seealso{
\code{\link[=ssb_codelist_details]{ssb_codelist_details()}} to inspect the codelist mapping.
\code{\link[=ssb_codelists]{ssb_codelists()}} to discover available codelists for a table.
\code{\link[=get_ssb_data]{get_ssb_data()}} for the \code{codelists} and \code{output_values} arguments
that produce the grouped codes this function expands.
}
