% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.r
\name{ssb_search}
\alias{ssb_search}
\title{Search SSB tables by keyword}
\usage{
ssb_search(
  query = NULL,
  language = "no",
  page = 1L,
  page_size = 20L,
  fetch_all = FALSE,
  as_tibble = TRUE
)
}
\arguments{
\item{query}{Optional character.  A keyword to search on.  The search
matches table titles.  Pass \code{NULL} (the default) to list all
available tables without filtering.}

\item{language}{Character.  Language for table titles and labels.
Use \code{"no"} for Norwegian (the default) or \code{"en"} for English.}

\item{page}{Integer.  Which page of results to return.  Defaults to
\code{1} (the first page).  SSB paginates search results; use
\code{fetch_all = TRUE} if you want all pages at once.}

\item{page_size}{Integer.  Number of tables per page.  Must be
between 1 and 1000.  Defaults to \code{20}.}

\item{fetch_all}{Logical.  If \code{TRUE}, all pages are fetched
automatically and combined into a single result.  Defaults to
\code{FALSE}.}

\item{as_tibble}{Logical.  If \code{TRUE} (the default), the result is
returned as a \link[tibble:tibble]{tibble}.}
}
\value{
A \link[tibble:tibble]{tibble} (or \code{data.frame} when
\code{as_tibble = FALSE}) with one row per matching table.
}
\description{
Searches the SSB Statistikkbank table catalogue and returns a summary
of matching tables.  Use this as the starting point when you do not
yet know the table identifier you need.
}
\details{
Each row in the result describes one table and contains the following
columns:
\describe{
\item{\code{id}}{The table identifier to pass to \code{\link[=get_ssb_data]{get_ssb_data()}}.}
\item{\code{label}}{The full title of the table.}
\item{\code{description}}{A short description (may be \code{NA}).}
\item{\code{updated}}{Date and time the table was last updated.}
\item{\code{first_period}, \code{last_period}}{The earliest and latest time
periods available.}
\item{\code{category}}{Thematic category (e.g. \code{"befolkning"}).
}
\item{\code{source}}{The SSB unit that publishes the table.}
\item{\code{variables}}{Comma-separated dimension names, excluding the
measure dimension which is present in every table.}
}

The result also carries four attributes describing the current page:
\code{page_number}, \code{page_size}, \code{total_elements}, and \code{total_pages}.
When \code{fetch_all = TRUE} the attributes reflect the last fetched page.
}
\examples{
\dontrun{
# Search for tables about population
ssb_search("befolkning")

# List all tables in English, 50 at a time
ssb_search(language = "en", page_size = 50)

# Retrieve all pages of results for a broad keyword
ssb_search("arbeid", fetch_all = TRUE)
}
}
\seealso{
\code{\link[=ssb_describe]{ssb_describe()}} to inspect the dimensions of a specific table.
\code{\link[=get_ssb_data]{get_ssb_data()}} to download data from a table.
}
