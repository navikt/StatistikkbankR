% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.r
\name{ssb_describe}
\alias{ssb_describe}
\title{Describe an SSB table and its dimensions}
\usage{
ssb_describe(table_id, language = "no", cache = TRUE, refresh_metadata = FALSE)
}
\arguments{
\item{table_id}{Character.  The SSB table identifier, for example
\code{"07459"}.}

\item{language}{Character.  Language for labels.  \code{"no"} (default)
or \code{"en"}.}

\item{cache}{Logical.  If \code{TRUE} (the default), table metadata is
stored in memory for the session so that repeated calls are fast.}

\item{refresh_metadata}{Logical.  If \code{TRUE}, discard any cached
metadata and fetch fresh information from SSB.  Defaults to
\code{FALSE}.}
}
\value{
A named list with elements \verb{$table} and \verb{$dimensions}.
See Details for the exact structure.
}
\description{
Returns a compact summary of a table's structure: its title, when it
was last updated, and a row for each dimension showing how many codes
are available and what role the dimension plays.  This is the
recommended first call when working with a new table.
}
\details{
The function returns a named list with two elements:
\describe{
\item{\verb{$table}}{A list with \code{table_id}, \code{table_label}, \code{updated}
(ISO timestamp), and \code{full_table_estimated_rows} (the total
number of cells if you were to download the whole table).}
\item{\verb{$dimensions}}{A \code{data.frame} with one row per dimension,
containing the dimension \code{id}, its \code{label}, its \code{role}, the
number of available codes (\code{n_codes}), and a logical column
\code{has_units} indicating whether unit metadata is available.}
}

The \code{role} column classifies each dimension:
\describe{
\item{\code{"time"}}{The primary time dimension.}
\item{\code{"geo"}}{A geographic dimension such as municipality or
county.}
\item{\code{"contents"}}{The measure dimension (what is being counted
or measured).}
\item{\code{"other"}}{Any other classification variable.}
}
}
\examples{
\dontrun{
desc <- ssb_describe("07459")

# Print the table-level summary
desc$table

# View the dimension list — use the 'dimension' column as
# the names of filter arguments in get_ssb_data()
desc$dimensions

# Same table with English labels
ssb_describe("07459", language = "en")
}
}
\seealso{
\code{\link[=ssb_search]{ssb_search()}} to find table identifiers.
\code{\link[=ssb_codes]{ssb_codes()}} to list valid filter values for a dimension.
\code{\link[=ssb_units]{ssb_units()}} to inspect measurement units.
}
